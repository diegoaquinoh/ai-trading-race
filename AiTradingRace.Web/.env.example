# AI Trading Race - Backend API Environment Variables
# 
# ⚠️  DEPRECATED: Use the root .env file instead
# This project now uses a single source of truth for all environment variables.
#
# Setup:
# 1. Copy the root .env file: cp ../.env.example ../.env
# 2. Edit ../.env with your configuration
# 3. Source it: source ../.env
# 4. Run the backend: dotnet run
#
# The backend will automatically load variables from the root .env file
# when using: source ../.env && dotnet run
#
# See: ../.env.example for all available configuration options
