# AI Trading Race - ML Service Environment Variables
#
# ⚠️  DEPRECATED: Use the root .env file instead
# This project now uses a single source of truth for all environment variables.
#
# Setup:
# 1. Copy the root .env file: cp ../.env.example ../.env
# 2. Edit ../.env with your configuration
# 3. docker-compose.yml automatically passes ML_SERVICE_* variables to the container
# 4. For local Python development: source ../.env && python -m app.main
#
# See: ../.env.example for all available ML_SERVICE_* configuration options
