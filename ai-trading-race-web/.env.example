# AI Trading Race - Frontend Environment Variables
#
# ⚠️  DEPRECATED: Use the root .env file instead
# This project now uses a single source of truth for all environment variables.
#
# Setup:
# 1. Copy the root .env file: cp ../.env.example ../.env
# 2. Edit ../.env with your configuration
# 3. The Vite build will automatically load VITE_* variables from root .env
# 4. Run frontend: npm run dev
#
# See: ../.env.example for all available VITE_* configuration options
