# AI Trading Race - Environment Variables (Single Source of Truth)
# Copy this file to .env and update with your values
# This file is used by:
# - docker-compose.yml (infrastructure)
# - scripts/*.sh (database scripts)
# - All application services via symlinks or env loading

# ============================================
# Database Configuration (SQL Server)
# ============================================
# SQL Server SA Password (matches docker-compose.yml)
SA_PASSWORD=YourStrong!Passw0rd

# SQL Server Container Name
SQL_CONTAINER_NAME=ai-trading-sqlserver

# SQL Server Database Name
SQL_DATABASE_NAME=AiTradingRace

# SQL Server Connection
SQL_SERVER=localhost
SQL_PORT=1433
SQL_USER=sa

# Connection String (for .NET applications)
ConnectionStrings__TradingDb="Server=localhost,1433;Database=AiTradingRace;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True"

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_TTL_SECONDS=3600

# ============================================
# ML Service Configuration
# ============================================
ML_SERVICE_API_KEY=test-api-key-12345
ML_SERVICE_MODEL_PATH=models/trading_model.pkl
ML_SERVICE_MODEL_VERSION=1.0.0
ML_SERVICE_LOG_LEVEL=INFO
ML_SERVICE_ALLOWED_ORIGIN=*
ML_SERVICE_REDIS_ENABLED=true
ML_SERVICE_REDIS_HOST=redis
ML_SERVICE_REDIS_PORT=6379
ML_SERVICE_REDIS_DB=0
ML_SERVICE_REDIS_TTL_SECONDS=3600

# Custom ML Service URL (for backend to call)
CustomMlAgent__BaseUrl=http://localhost:8000
CustomMlAgent__ApiKey=test-api-key-12345
CustomMlAgent__TimeoutSeconds=60

# ============================================
# Llama API Configuration (Groq)
# ============================================
Llama__Provider=Groq
Llama__BaseUrl=https://api.groq.com/openai/v1
Llama__Model=llama-3.3-70b-versatile
Llama__ApiKey=your-groq-api-key-here
Llama__TimeoutSeconds=60
Llama__MaxRetries=3

# ============================================
# CoinGecko API Configuration
# ============================================
# Optional - uses free tier without key
CoinGecko__ApiKey=
CoinGecko__BaseUrl=https://api.coingecko.com/api/v3/
CoinGecko__TimeoutSeconds=30
CoinGecko__DefaultDays=1

# ============================================
# Backend API Configuration (.NET)
# ============================================
ASPNETCORE_ENVIRONMENT=Development
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft_AspNetCore=Warning
AllowedOrigins="http://localhost:5173,http://localhost:3000"

# ============================================
# Frontend Configuration (React/Vite)
# ============================================
VITE_API_URL=http://localhost:5000
VITE_WS_URL=ws://localhost:5000/ws
VITE_ENABLE_WEBSOCKETS=false
VITE_ENABLE_DARK_MODE=true
VITE_ENABLE_ADVANCED_CHARTS=true
VITE_REFRESH_INTERVAL_MS=30000
VITE_LEADERBOARD_REFRESH_MS=10000
NODE_ENV=development

# ============================================
# Portfolio Configuration
# ============================================
# Starting balance for new portfolios
STARTING_BALANCE=100000.00

# ============================================
# Azure OpenAI (Legacy - Optional)
# ============================================
AzureOpenAI__Endpoint=
AzureOpenAI__ApiKey=
AzureOpenAI__DeploymentName=

# ============================================
# Usage Instructions
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Update SA_PASSWORD and Llama__ApiKey at minimum
# 3. Start Docker services: docker compose up -d
# 4. Source for scripts: source .env
# 5. Run database setup: ./scripts/setup-database.sh
# 6. Start backend: cd AiTradingRace.Web && dotnet run
# 7. Start frontend: cd ai-trading-race-web && npm run dev
#
# Note: docker-compose.yml automatically reads .env file
# The scripts and applications will use these same values for consistency.
